---
title: "Complete Guide to Testing Your MDX Renderer"
description: "A comprehensive test document to verify that your MDX renderer supports all formatting elements, including text styles, lists, code blocks, blockquotes, tables, custom components, and edge cases."
date: "2024-01-05"
---

# Complete Guide to Testing Your MDX Renderer

This is a comprehensive test document to verify that your MDX renderer is working correctly with all formatting elements.

## Basic Text Formatting

Here's a paragraph with **bold text**, *italic text*, and ***bold italic text***. You can also use `inline code` within sentences, and [links to external sites](https://example.com).

### Strikethrough and Other Elements

~~Strikethrough text~~ is also supported, along with superscript^2^ and subscript~2~.

## Lists

### Unordered Lists
- First item with some longer text to test wrapping
- Second item
  - Nested item one
  - Nested item two
    - Deeply nested item
- Third item with `inline code`

### Ordered Lists
1. First numbered item
2. Second numbered item with **bold text**
3. Third item
   1. Nested numbered item
   2. Another nested item
4. Fourth item with [a link](https://example.com)

### Task Lists
- [x] Completed task
- [ ] Incomplete task
- [x] Another completed task with `code`

## Code Examples

### Inline Code
Use `console.log()` to output debug information, or call `fetchData()` to retrieve information.

### Code Blocks

Basic code block:
```
This is a plain code block
with no syntax highlighting
```

JavaScript with syntax highlighting:
```javascript
function greetUser(name) {
  console.log(`Hello, ${name}!`);
  return `Welcome, ${name}`;
}

const user = "Alice";
const greeting = greetUser(user);
```

Python example:
```python
def calculate_fibonacci(n):
    if n <= 1:
        return n
    return calculate_fibonacci(n-1) + calculate_fibonacci(n-2)

# Generate first 10 fibonacci numbers
for i in range(10):
    print(f"F({i}) = {calculate_fibonacci(i)}")
```

CSS styling:
```css
.prose {
  max-width: 65ch;
  color: #374151;
}

.prose h1 {
  font-size: 2.25rem;
  font-weight: 800;
  margin-bottom: 1rem;
}

.prose code {
  background-color: #f3f4f6;
  padding: 0.125rem 0.25rem;
  border-radius: 0.25rem;
  font-size: 0.875rem;
}
```

## Blockquotes

> This is a simple blockquote that tests the styling of quoted content.

> This is a longer blockquote that spans multiple lines to test how the renderer handles longer quoted content. It should maintain proper spacing and indentation.
> 
> It can also contain **bold text**, *italic text*, and `inline code`.

### Nested Blockquotes

> This is the first level of quoting.
> 
> > This is a nested blockquote inside the first one.
> > 
> > > And this is a third level of nesting.

## Tables

| Feature | Status | Notes |
|---------|--------|-------|
| Headers | ✅ Working | H1-H6 all supported |
| Lists | ✅ Working | Ordered, unordered, nested |
| Code blocks | ⚠️ Testing | Syntax highlighting varies |
| Images | 🔄 In Progress | Need to test Cover component |
| Tables | ✅ Working | This table proves it! |

### Complex Table

| Syntax | Description | Example | Output |
|--------|-------------|---------|---------|
| `**bold**` | Bold text | `**hello**` | **hello** |
| `*italic*` | Italic text | `*world*` | *world* |
| `` `code` `` | Inline code | `` `console.log()` `` | `console.log()` |
| `[link](url)` | Hyperlink | `[Google](https://google.com)` | [Google](https://google.com) |

## Custom Components

### Cover Image Component
<Cover 
  src="https://picsum.photos/800/400?random=1" 
  alt="Sample landscape image for testing the Cover component" 
  caption="This tests the custom Cover component with a random landscape image"
/>

### Another Cover Image
<Cover 
  src="https://picsum.photos/600/300?random=2" 
  alt="Another sample image" 
  caption="A second image to test multiple Cover components in one document"
/>

## Horizontal Rules

Content above the rule.

---

Content below the rule.

---

## Special Characters and Symbols

Testing special characters: © ® ™ § ¶ † ‡ • … ‰ ′ ″ ‹ › « » ¡ ¿

Math symbols: ± × ÷ ∞ ≠ ≤ ≥ ≈ ∑ ∏ √ ∂ ∆ ∇

Arrows: ← → ↑ ↓ ↔ ↕ ↖ ↗ ↘ ↙

## Line Breaks and Spacing

This paragraph tests line breaks.  
This line has two spaces before the line break.

This paragraph has a manual line break.

This is a new paragraph with normal spacing.

## Mixed Content Test

Here's a complex example combining multiple elements:

1. **First point** with `inline code` and a [link](https://example.com)
2. *Second point* with a nested list:
   - Subitem with **bold text**
   - Another subitem with `more code`
3. Third point with a blockquote:
   > This quote is inside a list item and contains `inline code` as well.

## Edge Cases

### Empty Elements

Here's an empty code block:
```

```

Here's a blockquote with just whitespace:
>   

### Very Long Content

This is a very long paragraph designed to test text wrapping and ensure that the prose styles handle lengthy content appropriately. Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat. Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur.

### Long Code Line

```javascript
const veryLongVariableName = "This is a very long string that might cause horizontal scrolling issues in code blocks if not handled properly";
console.log(veryLongVariableName);
```

## Conclusion

This document tests:
- ✅ All heading levels (H1-H6)
- ✅ Text formatting (bold, italic, strikethrough)
- ✅ Lists (ordered, unordered, nested, tasks)
- ✅ Code blocks with syntax highlighting
- ✅ Blockquotes (simple and nested)
- ✅ Tables (simple and complex)
- ✅ Custom components (Cover)
- ✅ Links and inline code
- ✅ Horizontal rules
- ✅ Special characters
- ✅ Mixed content scenarios
- ✅ Edge cases

If all these elements render correctly, your MDX setup is working properly!